object FrontendCommands {
  val dependencyInstall: String = "npm install"
  val test: String = "npm run test"
  val buildDev: String = "npm run build-dev"
  val buildPrd: String = "npm run build-prd"
}
